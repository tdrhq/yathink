
/**
 * Copies the current thermals into ar. Returns the number of thermals
 * read if successful; returns 0 if unsuccessful or the number of
 * thermal values is greater than size.
 */
int thermal_get_temps (int* ar, int size);
